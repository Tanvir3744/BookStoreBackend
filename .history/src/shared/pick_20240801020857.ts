const pick =<T extends object, k extends keyof T> (obj, keys) => {
    const finalObject = {};
    for (const key of keys) {
        if(obj && Object.hasOwnproperty.call())
    }
};

export default pick;
